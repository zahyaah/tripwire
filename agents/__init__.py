"""Agents under test. This package never imports from `tripwire.assertions` or `tripwire.judge` —
the thing being measured must not see the measuring instrument (SPEC.md § Boundaries)."""

from __future__ import annotations
